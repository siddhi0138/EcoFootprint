# Loaded automatically by Vite only for production builds (npm run build), overriding matching
# keys from .env. Keeps local dev pointed at localhost while deployed builds hit the real backend.
VITE_API_BASE_URL=https://ecofootprint.onrender.com
