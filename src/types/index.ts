// src/types/index.ts
export interface IndexProps {
  showLoginForm: boolean;
  toggleLoginForm: () => void;
}
// src/types/index.ts

export interface IndexProps {
  showLoginForm: boolean;
  toggleLoginForm: () => void;
}